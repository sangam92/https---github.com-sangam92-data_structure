"""
Design an OrderedStream

"""
class OrderedStream:

    def __init__(self, n: int):
        self.n=n
        

    def insert(self, idKey: int, value: str) -> List[str]:
        res=[]
        for i in range(self.value):
            
            if idkey==i:
                if len(res)>0:
                    return res
                else:
                    return value
            else:
                res.append(value)